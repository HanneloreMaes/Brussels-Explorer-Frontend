import { Dimensions } from 'react-native';

const { width, height } = Dimensions.get('window');

export const cxEllipse = width / 2;
export const cyEllipse = '45';
export const rxEllipse = height / 1.8;
export const ryEllipse = height / 2;

export const svgHeight = height / 1.65;
export const imageWidth = width;
export const imageHeight = height / 1.6;
